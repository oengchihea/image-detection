{
  "version": 2,
  "buildCommand": "npm install --platform=linux --arch=x64 sharp --legacy-peer-deps && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "SHARP_IGNORE_GLOBAL_LIBVIPS": "true"
  }
}
{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npm install --no-save typescript @types/react @types/node @types/react-dom && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
}
